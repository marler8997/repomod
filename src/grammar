Root <- Statement*
Statement
   <- IfStatement
    / fn IDENTIFIER ParamDeclList Block
    / IDENTIFIER EQUAL Expr
    / Expr (EQUAL Expr)?
Expr  <- Expr2 (KEYWORD_or Expr2)*
Expr2 <- Expr3 (KEYWORD_and Expr3)*
...ExprX eventually becomes PrimaryExpr
PrimaryExpr
  <- IfExpr
   / KEYWORD_break Expr
   / KEYWORD_continue Expr
   / KEYWORD_return Expr
   / Block
   / PrimaryTypeExpr ExprSuffix

ExprSuffix <- (SuffixOp / FnCallArgs)*

PrimaryTypeExpr
    <- BUILTINIDENTIFIER FnCallArgs
     / NEW IDENTIFIER FnCallArgs
     / CHAR_LITERAL
     ??? / ContainerDecl
     / DOT IDENTIFIER
     ??? / DOT InitList
     ??? / ErrorSetDecl
     / FLOAT
     ??? / FnProto
     ??? / GroupedExpr
     ??? / LabeledTypeExpr
     / IDENTIFIER
     / IfTypeExpr
     / INTEGER
     / KEYWORD_unreachable
     / STRINGLITERAL

Block <- LBRACE Statement* RBRACE
//
SuffixOp
   <- LBRACKET Expr RBRACKET
    / DOT IDENTIFIER
//
IfExpr <- IfPrefix Expr ("else" Payload? Expr)?
IfPrefix <- "if" LPAREN Expr RPAREN PtrPayload?
FnCallArgs <- LPAREN (Expr ",")* Expr? RPAREN

ParamDeclList <- LPAREN (IDENTIFIER COMMA)* IDENTIFIER? RPAREN
